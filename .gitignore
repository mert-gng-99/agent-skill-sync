config.json
state.json
node_modules/
.DS_Store
.claude-project-id
extension/out/
extension/*.vsix

# packaged extension artifact
*.vsix

# This repo is itself a synced "project" from our own dogfooding - these are
# agent-sync's own generated output (skill mirrors, digest files), not tool
# source. They must never be committed or shipped in the packaged extension.
GEMINI.md
.agents/
